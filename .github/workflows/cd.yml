
name: .NET Core Desktop

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  setup:
    runs-on: windows-latest
    steps:
        - uses: actions/checkout@v3

        - name: Install msbuild
          uses: microsoft/setup-msbuild@v1.1

        - name: Build app
          run: msbuild FFXIVVoiceClipNameGuesser\FFXIVVoicePackCreator.csproj -p:Configuration=Release

#        - name: Setup dotNET 4.7.2
#          uses: actions/setup-dotnet@v3
#          with:
#            dotnet-version: 5.0.x

  #      - name: System.Resources.Extensions
  #        run: dotnet add package System.Resources.Extensions --version 7.0.0
        
        - name: Is stuff here?
          shell: cmd
          run: cd FFXIVVoiceClipNameGuesser\bin\Release && dir